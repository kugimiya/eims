# Скопируйте в .env и задайте свои значения (или запустите ./setup.sh).
# Хэш пароля содержит $ — в .env пишите его в одинарных кавычках: METRICS_PASSWORD_HASH='$2a$14$...'

METRICS_USER=metrics
METRICS_PASSWORD_HASH=
